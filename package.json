{
  "name": "reset28-mouvement",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:data": "node scripts/check-data.mjs",
    "check:animations": "node scripts/check-animations.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:public": "node scripts/check-public.mjs",
    "check:navigation": "node scripts/check-navigation.mjs",
    "check": "npm run build && npm run check:data && npm run check:animations && npm run check:links && npm run check:navigation"
  }
}
