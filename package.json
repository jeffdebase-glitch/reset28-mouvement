{
  "name": "reset28-mouvement",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:data": "node scripts/check-data.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check": "npm run build && npm run check:data && npm run check:links"
  }
}
